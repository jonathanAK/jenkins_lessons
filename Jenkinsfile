pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'build stage started'
                sh 'ls'
            }
        }
        stage('test') {
            steps {
                echo 'test stage started'
            }
        }
    }
}
